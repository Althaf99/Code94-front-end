import { css } from "@emotion/react";

const styles = () => {
  const section = css`
    padding-top: 10px;
    height: 100%;
    padding-bottom: 30px;
  `;

  return {
    section,
  };
};

export default styles;
